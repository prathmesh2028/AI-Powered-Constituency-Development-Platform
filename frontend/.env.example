# Environment Port Configuration
PORT=3000

# Backend Service API endpoint (Used at runtime for Server-Side Rendering (SSR) fetches and proxying)
# IMPORTANT: In Google Cloud Run, set this to the backend's fully qualified HTTPS URL.
BACKEND_URL=https://backend-service-xxxx-xx.a.run.app
