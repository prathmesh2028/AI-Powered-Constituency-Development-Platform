/**
 * Authentication Validator
 *
 * Validates signup, login, and profile updates.
 */

// TODO: Define validation schemas and middlewares
export const validateSignup = (req, res, next) => {
  next();
};

export const validateLogin = (req, res, next) => {
  next();
};
