/**
 * Issue Service
 *
 * Contains all business logic for constituency issues.
 * Services should:
 *  - Interact with models (DB queries)
 *  - Enforce business rules and validations
 *  - Orchestrate multi-step operations
 *  - Be callable from controllers OR other services
 *  - NOT access req/res objects (that's the controller's job)
 *
 * @example
 *  import Issue from "../models/issue.model.js";
 *
 *  export const createIssue = async (data) => {
 *    const issue = await Issue.create(data);
 *    return issue;
 *  };
 *
 *  export const getIssuesByConstituency = async (constituencyId, filters) => {
 *    return Issue.find({ constituency: constituencyId, ...filters })
 *      .sort({ createdAt: -1 })
 *      .lean();
 *  };
 */

// TODO: Implement service methods
